#include <stdio.h>
#include <stdlib.h>

int atoi(const char * str) {
    // int result=0;
    // int num;
    // bool negative = false;
    // size_t expnt = strlen(str);
    // size_t i, j;
    //
    // if(str[0] == '-'){
    //     negative = true;
    // }
    //
    // // Probably a better way to do this
    // for(i=expnt;i>0;i--){
    //     num = (int) str[i]-48;
    //     for(j=0;j<expnt;j++){
    //         num *= 10;
    //     }
    //     result += num;
    // }
    //
    // if(negative)
    //     result *= -1;
    // return result;
}
